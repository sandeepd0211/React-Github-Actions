name: CI
on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install Dependencies
        run: npm install

      - name: Lint Check
        run: npm run lint

  version-and-deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up Git
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"

      - name: Get the current version
        id: curr_version
        run: echo "::set-output name=version::$(node -pe "require('./package.json').version")"

      - name: Install Dependencies
        run: npm install

      - name: Build the project
        run: npm run build

      - name: Create or update version folder
        run: |
          mkdir -p ${{ steps.curr_version.outputs.version }}
          cp -r dist/* ${{ steps.curr_version.outputs.version }}

      - name: Commit changes to gh-pages
        run: |
          git checkout -B gh-pages
          git pull origin gh-pages
          git add .
          git commit -m "Deploy version ${{ steps.curr_version.outputs.version }}"
          git push origin gh-pages

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@4.1.5
        with:
          branch: gh-pages
          folder: ${{ steps.curr_version.outputs.version }}
